///
/// HybridStreamFetchBuilderSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `StreamFetchResponseInfo` to properly resolve imports.
namespace margelo::nitro::mobilefetch { struct StreamFetchResponseInfo; }
// Forward declaration of `HybridStreamFetchRequestSpec` to properly resolve imports.
namespace margelo::nitro::mobilefetch { class HybridStreamFetchRequestSpec; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include "StreamFetchResponseInfo.hpp"
#include <functional>
#include <memory>
#include "HybridStreamFetchRequestSpec.hpp"

namespace margelo::nitro::mobilefetch {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `StreamFetchBuilder`
   * Inherit this class to create instances of `HybridStreamFetchBuilderSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridStreamFetchBuilder: public HybridStreamFetchBuilderSpec {
   * public:
   *   HybridStreamFetchBuilder(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridStreamFetchBuilderSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridStreamFetchBuilderSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridStreamFetchBuilderSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void setMethod(const std::string& httpMethod) = 0;
      virtual void addHeader(const std::string& name, const std::string& value) = 0;
      virtual void setBodyString(const std::string& body) = 0;
      virtual void setBodyBytes(const std::shared_ptr<ArrayBuffer>& body) = 0;
      virtual void onResponse(const std::function<void(const StreamFetchResponseInfo& /* info */)>& callback) = 0;
      virtual void onChunk(const std::function<void(const std::shared_ptr<ArrayBuffer>& /* bytes */)>& callback) = 0;
      virtual void onComplete(const std::function<void()>& callback) = 0;
      virtual void onError(const std::function<void(const std::string& /* message */)>& callback) = 0;
      virtual std::shared_ptr<HybridStreamFetchRequestSpec> build() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "StreamFetchBuilder";
  };

} // namespace margelo::nitro::mobilefetch
