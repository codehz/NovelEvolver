///
/// HybridNativeSqliteSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `QueryResult` to properly resolve imports.
namespace margelo::nitro::mobilesqlite { struct QueryResult; }

#include <string>
#include "QueryResult.hpp"
#include <NitroModules/Null.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <variant>
#include <vector>

namespace margelo::nitro::mobilesqlite {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NativeSqlite`
   * Inherit this class to create instances of `HybridNativeSqliteSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNativeSqlite: public HybridNativeSqliteSpec {
   * public:
   *   HybridNativeSqlite(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNativeSqliteSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNativeSqliteSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNativeSqliteSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual double open(const std::string& name, const std::string& location, bool readonly) = 0;
      virtual QueryResult execute(double connectionId, const std::string& sql, const std::vector<std::variant<nitro::NullType, bool, std::shared_ptr<ArrayBuffer>, std::string, double>>& params) = 0;
      virtual void close(double connectionId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NativeSqlite";
  };

} // namespace margelo::nitro::mobilesqlite
