// Lower panes keep preferred heights; manuscript (first expanded) absorbs remaining body space via SidebarPaneStack geometry.
export const DEFAULT_MANUSCRIPT_BODY_HEIGHT = 168;
export const DEFAULT_REFERENCE_BODY_HEIGHT = 148;
export const DEFAULT_HISTORY_BODY_HEIGHT = 116;
